---
import Aperture from './Aperture.astro';
import { nav, site, withBase } from '../site';

const path = Astro.url.pathname;
// Compare against the based href — pathname includes the base in production.
const isCurrent = (href: string) => {
  const target = withBase(href);
  return path === target || path === target.replace(/\/$/, '');
};
---

<header class="nav-bar">
  <div class="nav-inner">
    <a class="brand" href={withBase('/')}>
      <Aperture size="26px" open={0.45} class="brand-mark" />
      <span class="brand-name">{site.name}</span>
    </a>

    <nav aria-label="Primary">
      <ul>
        {
          nav.map((item) => (
            <li>
              <a href={withBase(item.href)} aria-current={isCurrent(item.href) ? 'page' : undefined}>
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>

<style>
  .nav-bar {
    border-bottom: 1px solid var(--rule-soft);
  }

  .nav-inner {
    max-width: 1240px;
    margin-inline: auto;
    padding: 1.25rem clamp(1.25rem, 5vw, 4rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem 2rem;
    flex-wrap: wrap;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
  }

  .brand-name {
    font-family: var(--font-display);
    font-size: 0.78rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.2em;
  }

  .brand :global(.brand-mark) {
    transition: --open 900ms var(--ease-open);
  }

  .brand:hover :global(.brand-mark),
  .brand:focus-visible :global(.brand-mark) {
    --open: 1;
  }

  @media (prefers-reduced-motion: reduce) {
    .brand :global(.brand-mark) {
      transition: none;
    }
  }

  ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    gap: clamp(1rem, 3vw, 2.25rem);
    flex-wrap: wrap;
  }

  nav a {
    font-family: var(--font-display);
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    text-decoration: none;
    padding-block: 0.25rem;
    border-bottom: 1px solid transparent;
    white-space: nowrap;
  }

  nav a:hover {
    border-bottom-color: var(--color-quick);
  }

  nav a[aria-current='page'] {
    border-bottom-color: var(--color-quick);
  }

  @media (max-width: 430px) {
    .nav-inner {
      gap: 0.85rem;
    }

    ul {
      gap: 1rem;
    }

    nav a {
      font-size: 0.68rem;
      letter-spacing: 0.16em;
    }
  }
</style>
